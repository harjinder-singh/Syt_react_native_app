class Follower {
    constructor(id, follower, following, createdAt, updatedAt) {
        this.id = id,
        this.follower = follower
        this.following = following;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}

export default Follower;
  